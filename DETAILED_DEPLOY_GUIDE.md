# 详细部署指南：从零开始部署全栈应用

本文档将手把手教你如何将年会游戏网站完整地部署到公网。

## 📍 你的当前状态
- **前端代码**：看起来你已经尝试过 Vercel 部署 (本地有 `.vercel` 文件夹)，但可能还未配置正确连接到云端后端。
- **后端代码**：目前仅在本地运行，需要部署到云服务器。
- **数据库**：目前使用的是本地 MongoDB，需要迁移到云数据库。

---

## 第一阶段：准备代码仓库 (GitHub)
**这是所有部署的基础，必须先完成。**

1.  访问 [GitHub](https://github.com/) 并登录。
2.  点击右上角 `+` -> **New repository**。
3.  Repository name 填 `annual-party-game`，选择 **Private** (私有) 或 Public (公开)。
4.  点击 **Create repository**。
5.  在你的电脑上，使用终端 (Terminal) 执行以下命令将本地代码推送到 GitHub：
    ```bash
    git init
    git add .
    git commit -m "Initial commit"
    git branch -M main
    git remote add origin https://github.com/kazetear/annual-party-game
    git push -u origin main
    ```
    *(如果在推送过程中遇到权限问题，请确保已配置好 GitHub SSH Key 或使用 GitHub Desktop 工具上传)*

---

## 第二阶段：部署云数据库 (MongoDB Atlas)
**数据库是存放用户数据的地方，必须最先准备好。**

1.  **注册与创建**
    - 访问 [MongoDB Atlas 注册页面](https://www.mongodb.com/cloud/atlas/register)。
    - 注册后，选择 **Build a Database**。
    - 选择 **M0** (Free) 免费版，Provider 选 **AWS**，Region 选一个离你近的 (如 Hong Kong 或 Singapore，如果没有就选通用的)。
    - 点击 **Create**。

2.  **设置访问权限**
    - 创建数据库用户：Username 填 `admin`，Password 填一个复杂的密码 (例如 `Pass1234!`)。**一定要记下来！** 点击 **Create User**。
    - 设置网络访问：在 "Where would you like to connect from?" 选择 **My Local Environment**，然后点击下面的 **Add My Current IP Address**。
    - **重要**：为了让云服务器也能连上，再点击 **Add IP Address**，在 IP Address 框里填 `0.0.0.0/0` (表示允许任何 IP 连接)，点击 **Add Entry**。
    - 点击 **Finish and Close**。

3.  **获取连接字符串**
    - 在 Database Deployments 页面，点击 **Connect** 按钮。
    - 选择 **Drivers**。
    - 复制显示的连接字符串，它长这样：
      `mongodb+srv://admin:<password>@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0`
    - 把其中的 `<password>` 替换为你刚才设置的密码。
    - **保存这个字符串，下一步要用。**

---

## 第三阶段：部署后端服务器 (Railway)
**后端是游戏的核心大脑，负责处理 WebSocket 连接。**

1.  **导入项目**
    - 访问 [Railway](https://railway.app/) 并使用 GitHub 账号登录。
    - 点击 **New Project** -> **Deploy from GitHub repo**。
    - 选择你刚才上传的 `annual-party-game` 仓库。
    - 点击 **Deploy Now**。

2.  **配置环境变量**
    - 点击刚创建的项目卡片。
    - 进入 **Variables** 选项卡。
    - 点击 **New Variable**，添加：
        - 变量名: `MONGODB_URI`
        - 变量值: (粘贴你在第二阶段获取的 MongoDB 连接字符串)
    - 再添加一个：
        - 变量名: `PORT`
        - 变量值: `3001`
    - Railway 会自动重新部署。

3.  **生成公网域名**
    - 进入 **Settings** -> **Networking**。
    - 在 Public Networking 下，点击 **Generate Domain**。
    - 你会得到一个类似 `xxx-production.up.railway.app` 的域名。
    - **复制这个域名，下一步要用。**

---

## 第四阶段：更新前端部署 (Vercel)
**前端是用户看到的界面，需要告诉它去连接哪个后端。**

1.  **导入项目**
    - 访问 [Vercel Dashboard](https://vercel.com/dashboard)。
    - 点击 **Add New...** -> **Project**。
    - 在 Import Git Repository 处，找到你的 `annual-party-game` 仓库并点击 **Import**。

2.  **配置环境变量 (关键步骤)**
    - 在 Configure Project 页面，展开 **Environment Variables**。
    - 添加以下两个变量 (把 `你的Railway域名` 替换为第三阶段获得的域名，注意要加 `https://`)：
        - Key: `VITE_API_URL`
          Value: `https://你的Railway域名/api`
        - Key: `VITE_SOCKET_URL`
          Value: `https://你的Railway域名`
    - **注意**：URL 结尾**不要**带斜杠 `/`。

3.  **开始部署**
    - 点击 **Deploy**。
    - 等待约 1 分钟，屏幕上出现满屏彩带时，说明部署成功！
    - 点击 **Visit** 即可访问你的公网网站。

---

## 🎉 最终验证
1. 打开 Vercel 生成的网址。
2. 尝试创建一个新游戏。
3. 用手机（关闭 Wi-Fi 使用 4G/5G）扫描屏幕上的二维码。
4. 如果手机能成功加入房间并控制赛马，恭喜你，全栈部署完成！
